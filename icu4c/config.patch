diff --git a/icu4c/source/common/unicode/uconfig.h b/icu4c/source/common/unicode/uconfig.h
--- icu4c/source/common/unicode/uconfig.h
+++ icu4c/source/common/unicode/uconfig.h
@@ -88,9 +88,9 @@
  * Determines whether to disable renaming or not.
  * @internal
  */
 #ifndef U_DISABLE_RENAMING
-#define U_DISABLE_RENAMING 0
+#define U_DISABLE_RENAMING 1
 #endif
 
 /**
  * \def U_NO_DEFAULT_INCLUDE_UTF_HEADERS
diff --git a/icu4c/source/config/mh-msys-msvc b/icu4c/source/config/mh-msys-msvc
--- icu4c/source/config/mh-msys-msvc
+++ icu4c/source/config/mh-msys-msvc
@@ -52,10 +52,10 @@
 # -EHsc enables exception handling
 # -Zc:wchar_t makes wchar_t a native type. Required for C++ ABI compatibility.
 # -D_CRT_SECURE_NO_DEPRECATE is needed to quiet warnings about using standard C functions.
 # -utf-8 set source file encoding to utf-8.
-CFLAGS+=-GF -nologo -utf-8
-CXXFLAGS+=-GF -nologo -EHsc -Zc:wchar_t -utf-8
+CFLAGS+=-GF -nologo -utf-8 -Zc:inline -std:c17
+CXXFLAGS+=-GF -nologo -EHsc -Zc:wchar_t -utf-8 -std:c++17 -Zc:inline -Zc:__cplusplus -permissive-
 CPPFLAGS+=-D_CRT_SECURE_NO_DEPRECATE
 DEFS+=-DWIN32 -DCYGWINMSVC
 LDFLAGS+=-nologo
 
